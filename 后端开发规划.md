# Sync（随刻）数据 & 逻辑层 — 个人开发规划

## 一、定位：我负责什么

| 角色 | 负责人 | 分工 |
|------|--------|------|
| 前端 UI | 队友 | 页面界面、用户交互、导航切换 |
| **数据 & 逻辑层** | **我** | 本地存储、数据增删改查、自然语言解析、冲突检测、AI 提醒 |

Sync 是手机本地 App，数据存在手机上，不需要服务器。我的代码是一套**纯 Python 函数模块**，队友直接 `import` 调用即可。

---

## 二、技术选型

| 层面 | 选型 | 理由 |
|------|------|------|
| 编程语言 | Python 3 | 开发快、正则处理中文方便 |
| 本地存储 | **sqlite3**（Python 自带模块） | 零安装，单文件存储，关闭重开数据不丢 |
| 数据格式 | 字典/列表（Python 原生类型） | 无额外依赖 |
| 自然语言解析 | 正则表达式 + 关键词规则 | 按《本地规则.md》逐条实现 |

**不需要安装任何第三方库。**

---

## 三、整体调用关系

```
┌─────────────────────────────────────────────┐
│               前端 UI (队友写)                │
│          页面展示、用户点击、输入框             │
└─────────────────────┬───────────────────────┘
                      │ import / HTTP 调用函数
                      │ 返回字典或列表
┌─────────────────────▼───────────────────────┐
│            数据 & 逻辑层 (我写)               │
│                                              │
│  ┌──────────┐ ┌────────────┐ ┌───────────┐  │
│  │  db.py   │ │timeline.py │ │  nlu.py   │  │
│  │ 增删改查  │ │排序+冲突检测│ │自然语言解析│  │
│  └────┬─────┘ └─────┬──────┘ └─────┬─────┘  │
│       │             │              │         │
│       └──────┬──────┴──────────────┘         │
│              ▼                                │
│         sync.db (SQLite 文件)                 │
└──────────────────────────────────────────────┘
```

---

## 四、开发前需要和队友（前端开发）对接确定的事项

在动手写代码之前，以下事项必须和队友沟通并达成一致，否则联调时会出现大量对接问题。

### 4.1 调用方式

| 问题 | 说明 | 需要确定的选项 |
|------|------|--------------|
| 前端技术栈是什么？ | 决定了我的代码如何被调用 | A) Python 框架（Kivy/BeeWare）→ 直接 import；B) 其他语言 → 需要从我的代码暴露 HTTP 接口 |
| 如果走 HTTP，端口用哪个？ | 避免和前端已有服务冲突 | 建议 `http://localhost:8800` |
| 函数调用是同步还是异步？ | 影响前端调用写法 | 建议同步（数据库操作瞬间完成，无需异步） |

### 4.2 数据字段格式

| 字段 | 格式约定 | 示例 | 为什么重要 |
|------|---------|------|-----------|
| `date` | `YYYY-MM-DD` 字符串 | `"2026-07-03"` | 前后端查询/展示都用这个格式，不一致会导致查不到数据 |
| `start_time` / `end_time` | `HH:MM` 字符串（24小时制） | `"08:00"`、`"22:30"` | 排序和冲突检测都依赖时间比较 |
| `duration` | 整数（分钟） | `90` | 前端展示"1小时30分钟"由前端自行转换 |
| `priority` | `"P0"` / `"P1"` / `"P2"` 字符串 | `"P0"` | 后端只返回标签，颜色由前端决定 |
| `status` | `"pending"` / `"completed"` | `"pending"` | 前端根据状态显示不同样式 |
| `repeat` | `"none"` / `"daily"` / `"weekly"` / `"weekdays"` / `"weekends"` / `"monthly"` | `"daily"` | 重复日程的展开逻辑由前端实现 |

### 4.3 函数返回值格式

统一使用以下格式，成功和失败都走同一个结构：

```json
// 成功
{
  "ok": true,
  "data": { ... }
}

// 失败
{
  "ok": false,
  "error": "具体错误原因（中文）"
}
```

**队员需要提前约定好：拿到返回值后先判断 `ok` 字段，再决定是取 `data` 还是展示 `error`。**

### 4.4 日程 ID 生成方式

| 方案 | 优点 | 缺点 |
|------|------|------|
| **数据库自增 ID**（推荐） | 简单可靠，不会重复 | 前端创建后需要等返回才能拿到 id |
| 前端生成 UUID | 可离线创建 | 增加前端复杂度，需要传 uuid 字段 |

建议用**数据库自增 ID**：前端调用创建函数 → 返回带 `id` 的完整日程 → 前端保存这个 `id`，后续编辑/删除时传回。

### 4.5 交互流程确认

以下交互流程需要前后端各自知道自己该做什么：

**手动创建日程流程：**
```
用户点击 + → 前端弹出表单 → 用户填写 → 前端调 create_schedule(data)
→ 我返回 {ok:true, data:{id:1, ...}}
→ 前端关闭表单，重新调 get_timeline(date) 刷新时间轴
```

**自然语言输入流程：**
```
用户输入文字 → 前端调 nlu.parse(text, today)
→ 我返回解析结果 {ok:true, data:{intent, draft, confidence}}
→ 前端展示确认卡片 → 用户点"确认添加"
→ 前端调 db.create_schedule(draft) → 刷新时间轴 → 调 reminders.check(date)
```

**日程编辑/删除流程：**
```
用户点击日程卡片 → 前端展示详情弹窗 → 用户操作
→ 编辑：前端调 db.update_schedule(id, new_data)
→ 删除：前端弹窗确认 → 调 db.delete_schedule(id)
→ 标记完成：前端调 db.mark_complete(id)
→ 操作完成后重新调 get_timeline(date) 刷新
```

**二次确认机制：**
- 删除操作：**前端负责弹窗确认**，确认后才调用删除函数。后端不做二次拦截。
- 顺延操作：同理，前端确认后再调用。
- NLU 草稿：前端展示确认卡片，确认后才调创建函数。

### 4.6 优先级颜色

| 优先级 | 前端展示建议 | 后端输出 |
|--------|------------|---------|
| P0 | 最深色（如深蓝或深红） | 只返回字符串 `"P0"` |
| P1 | 中等色 | 只返回字符串 `"P1"` |
| P2 | 最浅色 | 只返回字符串 `"P2"` |

具体色值由前端决定，后端只负责返回优先级标签。

### 4.7 首次启动与空状态

| 场景 | 后端行为 | 前端行为 |
|------|---------|---------|
| App 首次启动 | `init_db()` 自动建表，无数据 | 展示空状态文案："今天还没有安排，告诉 Life 你想做什么" |
| 当天无日程 | `get_timeline(date)` 返回空列表 | 展示空状态 + 输入框 |
| 当天有日程 | `get_timeline(date)` 返回日程列表 | 展示时间轴 |

---

## 五、初步开发中需要实现的代码文件及其功能

### 5.1 文件总数概览

| 阶段 | 文件 | 说明 |
|------|------|------|
| **P0（核心）** | 3 个 | `db.py`、`timeline.py`、`test_db.py` |
| **P1（智能）** | 3 个 | `nlu.py`、`reminders.py`、`test_nlu.py` |
| **P2（完善）** | 1 个 | `modes.py` |

核心逻辑共 **6 个 `.py` 文件**（不含测试文件为 **4 个**），每个文件职责单一。

---

### 5.2 db.py — 数据库操作（P0）

**职责：** 所有对 SQLite 的读写操作，是其他模块的基础。

**包含函数：**

| 函数 | 输入 | 返回 | 说明 |
|------|------|------|------|
| `init_db(db_path)` | `"sync.db"` | `{"ok": true}` | 创建数据库文件和 schedules 表，如果已存在则跳过。App 启动时调用一次 |
| `create_schedule(data)` | `{"title":"跑步","date":"2026-07-03","start_time":"08:00","duration":45,"priority":"P2"}` | `{"ok":true, "data":{...含id...}}` | 校验必填字段（标题、日期、时间非空），自动计算 `end_time`，写入数据库 |
| `get_schedules(date)` | `"2026-07-03"` | `{"ok":true, "data":[{...},{...}]}` | 按日期查询所有日程，按 `start_time` 升序返回 |
| `get_schedule(id)` | `1` | `{"ok":true, "data":{...}}` | 按 id 查单条，不存在返回 `{"ok":false, "error":"日程不存在"}` |
| `update_schedule(id, data)` | `1, {"title":"新标题"}` | `{"ok":true, "data":{...}}` | 更新指定 id 的日程字段，自动更新 `updated_at` |
| `delete_schedule(id)` | `1` | `{"ok":true}` | 删除指定 id 的日程 |
| `mark_complete(id)` | `1` | `{"ok":true, "data":{...}}` | 将 status 改为 `"completed"` |
| `postpone_schedule(id)` | `1` | `{"ok":true, "data":{...}}` | 将 date 改为明天（today + 1 天） |

**数据校验规则（写在 create_schedule 和 update_schedule 内）：**
- `title` 不能为空
- `date` 不能为空，必须是 `YYYY-MM-DD` 格式
- `start_time` 不能为空，必须是 `HH:MM` 格式
- `duration` 必须是正整数（> 0）
- `priority` 必须是 `"P0"` / `"P1"` / `"P2"` 之一
- 校验不通过直接返回 `{"ok": false, "error": "具体原因"}`

---

### 5.3 timeline.py — 时间轴排序 + 冲突检测（P0）

**职责：** 从 db 拿到日程列表后，排序并检测冲突。

**包含函数：**

| 函数 | 输入 | 返回 | 说明 |
|------|------|------|------|
| `get_timeline(date)` | `"2026-07-03"` | `{"ok":true, "data":{"date":"...","items":[...]}}` | 主函数：获取当天日程 → 排序 → 冲突检测 → 返回结构化时间轴 |
| `_is_overlap(a, b)` | 两个日程字典 | `True/False` | 判断两个日程时间段是否重叠：`max(a.start, b.start) < min(a.end, b.end)` |
| `_group_conflicts(schedules)` | 已排序的日程列表 | 分组后的列表 | 内部函数，将重叠日程分组为 single / conflict_pair / conflict_group |

**返回结构：**

```json
{
  "ok": true,
  "data": {
    "date": "2026-07-03",
    "items": [
      {
        "type": "single",
        "schedule": { "id": 1, "title": "晨跑", "start_time": "08:00", "end_time": "08:45", "priority": "P2", "status": "pending" }
      },
      {
        "type": "conflict_pair",
        "schedules": [
          { "id": 2, "title": "学习", "start_time": "15:00", "end_time": "16:30", "priority": "P1" },
          { "id": 3, "title": "小组会议", "start_time": "15:30", "end_time": "16:00", "priority": "P0" }
        ]
      },
      {
        "type": "conflict_group",
        "schedules": [ /* 3 个及以上重叠的日程 */ ]
      }
    ]
  }
}
```

**冲突分组规则：**

| 重叠数 | type | 前端展示 |
|--------|------|---------|
| 1 个（无冲突） | `single` | 普通日程卡片 |
| 2 个 | `conflict_pair` | 并列卡片 |
| 3 个及以上 | `conflict_group` | 折叠卡片，点击展开二级菜单 |

---

### 5.4 nlu.py — 自然语言解析引擎（P1）

**职责：** 把用户输入的自然语言转换成结构化日程草稿。

**包含函数：**

| 函数 | 输入 | 返回 | 说明 |
|------|------|------|------|
| `parse(text, today_date)` | `"明天早上8点跑步45分钟"`, `"2026-07-03"` | `{"ok":true, "data":{...}}` | 主函数：调用 11 步解析流水线，返回意图和日程草稿 |

**返回结构：**

```json
{
  "ok": true,
  "data": {
    "intent": "create_schedule",
    "draft": {
      "title": "跑步",
      "date": "2026-07-04",
      "start_time": "08:00",
      "end_time": "08:45",
      "duration": 45,
      "priority": "P2",
      "repeat": "none",
      "need_confirmation": true
    },
    "confidence": 0.95,
    "parsed_details": {
      "date_raw": "明天",
      "time_raw": "早上8点",
      "duration_raw": "45分钟",
      "action_raw": "跑步"
    }
  }
}
```

**11 步解析流水线（按《本地规则.md》逐步实现）：**

```
步骤1  clean(text)             → 文本清洗：去多余标点、统一大小写、中文数字转阿拉伯数字
步骤2  detect_intent(text)     → 意图判断：create / update / delete / postpone / start_mode / unknown
步骤3  parse_date(text, today) → 日期识别：今天→today / 明天→today+1 / 周一→本周一 / 下周三→下周
步骤4  parse_time(text)        → 时间识别：早上8点→08:00 / 下午3点→15:00 / 晚上→20:00 / 模糊→默认
步骤5  parse_duration(text)    → 时长识别：30分钟→30 / 1小时→60 / 一个半小时→90 / 无→按类型默认
步骤6  parse_repeat(text)      → 重复识别：每天→daily / 每周→weekly / 工作日→weekdays / 无→none
步骤7  parse_priority(text)    → 优先级识别：deadline/考试→P0 / 学习/开会→P1 / 休息→P2 / 无→P1
步骤8  extract_title(text)     → 移除已识别的时间/日期/频次/优先级词，剩余部分作为标题
步骤9  fill_defaults(draft)    → 补默认值：缺日期→今天 / 缺时间→按类型默认 / 缺优先级→P1 / 缺时长→按类型默认
步骤10 validate(draft)         → 校验：标题非空、日期合法、时间合法
步骤11 score(draft)            → 置信度打分：全明确→0.9 / 部分模糊→0.7 / 缺关键字段→0.4
```

**意图类型及处理策略：**

| intent | 含义 | 处理方式 |
|--------|------|---------|
| `create_schedule` | 创建新日程 | 返回 draft，前端展示确认卡片 |
| `update_schedule` | 修改已有日程 | 返回 target（匹配的日程）+ new_fields，前端二次确认 |
| `delete_schedule` | 删除日程 | 返回 target，前端展示删除确认弹窗 |
| `postpone_schedule` | 顺延日程 | 返回 target + new_date，前端确认 |
| `start_mode` | 开启模式 | 返回 mode_name + duration |
| `unknown` | 无法解析 | 返回提示信息，引导用户手动补充或换说法 |

**置信度分层处理：**

| 置信度 | 处理方式 |
|--------|---------|
| ≥ 0.85 | 直接展示确认卡片，用户可一键确认 |
| 0.60 ~ 0.85 | 展示确认卡片，但高亮不确定的字段，允许用户修改 |
| < 0.60 | 不生成确认卡片，返回提示文案，引导用户进入手动新建页 |

---

### 5.5 reminders.py — AI 提醒检测（P1）

**职责：** 每次日程变更后检测是否需要提醒用户。

**包含函数：**

| 函数 | 输入 | 返回 | 说明 |
|------|------|------|------|
| `check_reminders(date)` | `"2026-07-03"` | `{"ok":true, "data":[{...}]}` | 主函数：遍历当天日程，按 6 条规则逐条检测，返回提醒列表 |

**6 条检测规则（按严重程度排序）：**

| 优先级 | 规则 | 触发条件 | 提醒文案示例 | 建议文案 |
|--------|------|---------|-------------|---------|
| 1 | 时间冲突 | 两个日程时间段重叠 | "「学习」和「小组会议」时间冲突（15:00-16:00）" | "建议将「学习」顺延到 16:30" |
| 2 | 晚间 P0 | 20:00 之后有 P0 任务 | "今晚有高优先级任务「Deadline」" | "需要的话可以开启专注模式" |
| 3 | 任务超时 | 单日程超过 2 小时 | "「学习」已持续超过 2 小时" | "请注意休息" |
| 4 | 结束过晚 | 有日程结束时间晚于 23:00 | "「Deadline」预计 23:30 结束" | "是否需要调整到明天" |
| 5 | 连续学习过久 | 连续多个学习类日程总时长超 90 分钟 | "学习类任务已累计 2.5 小时" | "建议安排一次休息" |
| 6 | 未完成任务 | 当天有未完成的 pending 任务且当前时间已过日程结束时间 | "「读书」未完成" | "点击顺延到明天" |

**返回结构：**

```json
{
  "ok": true,
  "data": [
    {
      "type": "time_conflict",
      "priority": 1,
      "message": "「学习」和「小组会议」时间冲突（15:00-16:00）",
      "suggestion": "建议将「学习」顺延到 16:30",
      "related_schedule_ids": [2, 3]
    }
  ]
}
```

**调用时机：** 前端在以下操作成功后调用 `check_reminders(date)`：
- 创建日程后
- 编辑日程后
- 删除日程后
- 标记完成后

---

### 5.6 modes.py — 预设模式（P2，后期）

| 函数 | 作用 |
|------|------|
| `start_mode(mode_name)` | 开启学习/放松/小憩模式，返回开始时间和预计结束时间 |
| `end_mode(mode_name)` | 结束模式，返回正向反馈文案 |

预设模式在 MVP 阶段只需一个基础结构，P2 再完善。

---

### 5.7 测试文件

| 文件 | 测试内容 |
|------|---------|
| `test_db.py` | 创建/查询/更新/删除/标记完成/顺延，以及校验（空标题/非法优先级） |
| `test_nlu.py` | 运行《本地规则.md》中的 20 条 MVP 测试语句，逐条验证输出 |
| `test_timeline.py` | 无冲突/2 个冲突/3 个冲突/边界情况 |

---

## 六、开发顺序与步骤

### 第 1 阶段（P0）：日程能存、能查、时间轴能排序

> **目标：** 完成最小可用闭环——手动创建日程 → 查当天日程 → 按时间排序展示

---

#### 步骤 1：搭建项目骨架

1. 创建项目文件夹 `sync-logic/`
2. 创建空文件 `db.py`、`timeline.py`、`test_db.py`
3. 确认 Python 3 可用（终端执行 `python --version`）

**产出物：** 3 个空 `.py` 文件

---

#### 步骤 2：实现 db.py — 建表与创建日程

**2.1 先写 `init_db()`：**
- 用 `sqlite3.connect()` 连接 `sync.db`
- 执行 `CREATE TABLE IF NOT EXISTS schedules (...)` 建表语句
- 执行 `CREATE INDEX IF NOT EXISTS ...` 建索引
- 返回 `{"ok": true}`

**验收：** 运行 `init_db()` 后在目录下生成 `sync.db` 文件

**2.2 再写 `create_schedule(data)`：**
- 校验 title / date / start_time 非空
- 校验 priority 是 P0/P1/P2
- 校验 duration 是正整数
- 根据 `start_time` 和 `duration` 计算 `end_time`
- 填入 `created_at` 和 `updated_at`（当前时间 ISO 格式）
- 插入数据库，返回带 id 的完整日程

**验收：** 
```python
r = create_schedule({"title": "晨跑", "date": "2026-07-03", "start_time": "08:00", "duration": 45, "priority": "P2"})
# 期望: r == {"ok": True, "data": {"id": 1, "title": "晨跑", "date": "2026-07-03", "start_time": "08:00", "end_time": "08:45", ...}}
```

---

#### 步骤 3：实现 db.py — 查询与删除

**3.1 写 `get_schedules(date)`：**
- 按 date 筛选 + start_time 升序查询
- 返回日程列表（空列表也正常返回）

**3.2 写 `get_schedule(id)`：**
- 精确按 id 查询
- 不存在返回 `{"ok": false, "error": "日程不存在"}`

**3.3 写 `delete_schedule(id)`：**
- 按 id 删除
- 确认行数变化，无变化返回错误

**3.4 写 `update_schedule(id, data)`：**
- 按 id 更新传入字段
- 自动更新 `updated_at`
- 重新计算 `end_time`（如果 start_time 或 duration 有变化）

**3.5 写 `mark_complete(id)` 和 `postpone_schedule(id)`：**
- mark_complete：将 status 改为 `"completed"`
- postpone：将 date 改为 `today + 1`（需要接收当前日期参数或自动获取）

---

#### 步骤 4：实现 timeline.py — 排序与冲突检测

**4.1 写 `_is_overlap(a, b)`：**

```python
def _is_overlap(a, b):
    """判断两个日程的时间段是否重叠"""
    return max(a["start_time"], b["start_time"]) < min(a["end_time"], b["end_time"])
```

**4.2 写 `_group_conflicts(schedules)`：**
- 输入：已按 start_time 排序的日程列表
- 遍历日程，用 `_is_overlap` 判断相邻日程是否重叠
- 1 个 → `{"type": "single", "schedule": ...}`
- 2 个重叠 → `{"type": "conflict_pair", "schedules": [...]}`
- 3+ 个重叠 → `{"type": "conflict_group", "schedules": [...]}`

**4.3 写 `get_timeline(date)`：**
- 调用 `db.get_schedules(date)` 获取日程列表
- 调用 `_group_conflicts()` 处理
- 返回统一结构

---

#### 步骤 5：写 test_db.py 验证

**测试用例：**

| 测试场景 | 操作 | 预期结果 |
|---------|------|---------|
| 正常创建 | `create_schedule(valid_data)` | 返回 ok，含 id |
| 缺标题 | `create_schedule({title:""})` | 返回 error |
| 缺日期 | `create_schedule({date:""})` | 返回 error |
| 非法优先级 | `create_schedule({priority:"P99"})` | 返回 error |
| 查询当天 | `get_schedules("2026-07-03")` | 返回刚才创建的日程列表 |
| 查询无数据日 | `get_schedules("2026-01-01")` | 返回空列表 |
| 更新标题 | `update_schedule(1, {title:"新标题"})` | 返回更新后的日程 |
| 标记完成 | `mark_complete(1)` | status 变为 "completed" |
| 删除 | `delete_schedule(1)` | 返回 ok |
| 删除后再查 | `get_schedule(1)` | 返回 error |

---

#### 步骤 6：写 test_timeline.py 验证

**测试用例：**

| 测试场景 | 操作 | 预期 |
|---------|------|------|
| 无冲突 | 创建 08:00-09:00 和 10:00-11:00 | items 全是 single |
| 2 个完全重叠 | 创建 15:00-16:00 和 15:00-16:00 | 出现 conflict_pair |
| 2 个部分重叠 | 创建 15:00-16:30 和 15:30-16:00 | 出现 conflict_pair |
| 3 个重叠 | 创建 15:00-16:00、15:30-16:00、15:45-16:00 | 出现 conflict_group |
| 边界不重叠 | 创建 08:00-09:00 和 09:00-10:00 | items 全是 single（end=start 不重叠） |

---

#### P0 阶段完成标志

- `test_db.py` 所有用例通过
- `test_timeline.py` 所有用例通过
- 队友可以用 `db.create_schedule()` 和 `timeline.get_timeline()` 完成"添加日程 → 首页展示时间轴"

---

### 第 2 阶段（P1）：自然语言解析 + AI 提醒

> **目标：** 输入"今晚 10 点 deadline" → 生成 P0 任务 → 触发晚间提醒

---

#### 步骤 7：实现 nlu.py — 文本清洗（步骤 1）

**任务：** 写 `_clean(text)` 函数

- 去除多余标点符号（`。。。` → 空、`！！！` → 空、`，，，` → 空）
- 英文统一小写（`Deadline` → `deadline`）
- 中文数字转阿拉伯数字：维护一个映射字典
  ```
  "一"→1, "二"→2, ..., "十"→10,
  "半"→0.5, "半小时"→"30分钟", "一个半小时"→"1.5小时"
  ```

**验收：**
```python
_clean("明天！！！下午3点，，，学习90分钟")  # → "明天下午3点学习90分钟"
_clean("Deadline")                          # → "deadline"
_clean("一个半小时")                         # → "1.5小时"
```

---

#### 步骤 8：实现 nlu.py — 意图识别（步骤 2）

**任务：** 写 `_detect_intent(text)` 函数

按关键词优先级匹配（先匹配破坏性操作，再匹配创建操作）：

| 优先级 | 关键词 | 意图 |
|--------|--------|------|
| 1 | 取消/删除/删掉/不要了/不去了/不用了 | `delete_schedule` |
| 2 | 顺延/明天再做/以后再做/推后/挪到明天 | `postpone_schedule` |
| 3 | 改到/调整到/挪到/换到/提前到/推迟到/延后到/改成 | `update_schedule` |
| 4 | 学习模式/专注模式/放松模式/小憩模式/休息一下 | `start_mode` |
| 5 | 有具体任务名 + 时间词 | `create_schedule` |
| 默认 | 以上都不匹配 | `unknown` |

---

#### 步骤 9：实现 nlu.py — 日期、时间、时长、重复、优先级解析（步骤 3-7）

**日期解析 `_parse_date(text, today)`：**

| 输入 | 输出 |
|------|------|
| 今天 / 今晚 | today |
| 明天 / 明早 / 明晚 | today + 1 天 |
| 后天 | today + 2 天 |
| 大后天 | today + 3 天 |
| 周一/星期一/礼拜一 | 本周一（已过则下周） |
| 下周一 | 下周的周一 |
| 周五 | 本周五（已过则下周） |
| 无日期 | today |

**时间解析 `_parse_time(text)`：**

| 输入 | 输出（HH:MM） |
|------|-------------|
| 早上8点 | `08:00` |
| 上午9点 | `09:00` |
| 中午12点 | `12:00` |
| 下午3点 | `15:00` |
| 傍晚6点 | `18:00` |
| 晚上8点 | `20:00` |
| 晚上10点 | `22:00` |
| 睡前 | `22:30` |
| 3点到5点 | start `15:00`, end `17:00`（直接计算 duration） |
| 没有时间 | 按模糊时间默认值表（本地规则 六.3） |

**时长解析 `_parse_duration(text)`：**
- 明确时长：`30分钟`→30, `1小时`→60, `一个半小时`→90
- 时间段反推：`3点到5点` → `17:00 - 15:00` = 120 分钟
- 无时长的默认值按任务类型：学习→90, 会议→60, 跑步→45, 休息→30

**重复解析 `_parse_repeat(text)`：**
- 每天/每日/每晚→`daily`
- 每周一/每周五→`weekly`
- 工作日/每个工作日→`weekdays`
- 周末→`weekends`

**优先级解析 `_parse_priority(text)`：**
- P0 关键词：deadline/ddl/截止/必须/紧急/考试/面试/提交/交作业/汇报
- P2 关键词：休息/放松/小憩/午睡/散步/听歌/看剧/不急/随便/有空
- 其余默认 P1

---

#### 步骤 10：实现 nlu.py — 标题提取、默认值、校验、置信度（步骤 8-11）

**标题提取：** 把已识别的日期/时间/时长/优先级关键词逐一从原文中移除，剩余部分作为标题。如果为空 → confidence 降级。

**默认值填充：**

| 缺失字段 | 默认值 |
|---------|--------|
| date | 今天 |
| time | 按任务类型默认（学习 09:00, 读书 21:00, 跑步 08:00） |
| duration | 按任务类型默认（学习 90, 会议 60, 跑步 45, 休息 30） |
| priority | P1 |
| repeat | none |

**置信度规则：**

| 条件 | 置信度 |
|------|--------|
| 标题 + 日期 + 时间 + 时长全部明确 | 0.90 ~ 0.95 |
| 标题明确但时间模糊（如"晚上"） | 0.75 ~ 0.85 |
| 标题明确但缺时间 | 0.65 ~ 0.75 |
| 标题模糊（如"那个"）或缺标题 | 0.30 ~ 0.50 |
| 完全无法解析 | 0.00 ~ 0.30 |

---

#### 步骤 11：实现 nlu.py — 主函数 `parse()`

把步骤 7~10 串联，返回统一结构。增加 `need_confirmation` 标记：
- `delete_schedule` / `update_schedule` / `postpone_schedule` → 强制为 true
- `create_schedule` 且置信度 > 0.85 → true（仍需确认，但不强制修改）
- 置信度 < 0.6 → 不生成草稿，直接返回提示

---

#### 步骤 12：写 test_nlu.py — 用 20 条 MVP 用例验证

逐条对照《本地规则.md》第十七节的测试用例，验证每条输入是否正确解析：

| # | 输入 | 预期 |
|---|------|------|
| 1 | 明天早上8点跑步 | create_schedule, 明天 08:00, 跑步 |
| 2 | 今天下午3点学习90分钟 | create_schedule, 今天 15:00, 学习, 90min |
| 3 | 晚上10点deadline | create_schedule, 今天 22:00, Deadline, P0 |
| 4 | 今晚有deadline，晚点睡 | create_schedule, P0, 触发加班建议 |
| 5 | 每天晚上读书45分钟 | create_schedule, daily, 21:00, 读书, 45min |
| 6 | 周五下午4点小组会议 | create_schedule, 周五 16:00, 小组会议 |
| 7 | 下午2点到4点复习数学 | create_schedule, 14:00-16:00, 复习数学 |
| 8 | 半小时后休息 | create_schedule, 当前时间+30min, 休息 |
| 9 | 明天考试 | create_schedule, 明天, 考试, P0, 缺时间 |
| 10 | 取消今晚跑步 | delete_schedule |
| 11 | 把学习改到晚上8点 | update_schedule |
| 12 | 今天太累了，晚读明天再做 | postpone_schedule |
| 13 | 开启学习模式 | start_mode |
| 14 | 小憩20分钟 | start_mode, 20min |
| 15 | 明天晚上写作业1小时 | create_schedule, 明天 20:00, 60min |
| 16 | 每周一晚上跑步 | create_schedule, weekly, 20:00 |
| 17 | 有空再整理书桌 | create_schedule, P2, 缺时间 |
| 18 | 下午提交作业 | create_schedule, 15:00, P0 |
| 19 | 睡前背单词30分钟 | create_schedule, 22:30, 30min |
| 20 | 晚点弄一下那个 | confidence < 0.6, unknown |

---

#### 步骤 13：实现 reminders.py

按 6 条规则逐条实现，每条返回匹配结果。`check_reminders(date)` 汇总所有结果按优先级排序返回。

---

#### P1 阶段完成标志

- `test_nlu.py` 20 条用例全部通过
- 可以演示：输入"今晚 10 点 deadline" → 前端展示确认卡片 → 确认 → 首页出现 P0 日程 → 出现晚间 P0 提醒

---

### 第 3 阶段（P2）：完善与打磨

> 有余力再做

| 步骤 | 内容 |
|------|------|
| 14 | 实现 `modes.py`（预设模式：学习/放松/小憩） |
| 15 | 实现操作历史记录 |
| 16 | 实现设置存储（默认优先级、默认时长） |
| 17 | 正向反馈文案（模式结束后返回鼓励文案） |
| 18 | 全流程集成测试 |

---

### 开发顺序总览图

```
P0: 基础闭环（预计 3 天）
  ├─ 步骤1：项目骨架
  ├─ 步骤2：db.init_db + db.create_schedule
  ├─ 步骤3：db 查询/删除/更新/标记完成/顺延
  ├─ 步骤4：timeline 排序 + 冲突检测
  ├─ 步骤5：test_db.py 验证
  └─ 步骤6：test_timeline.py 验证
       │
       ▼  P0 验收：手动创建 → 时间轴展示 ← 与队友联调
       │
P1: 智能核心（预计 4 天）
  ├─ 步骤7：nlu 文本清洗
  ├─ 步骤8：nlu 意图识别
  ├─ 步骤9：nlu 日期/时间/时长/重复/优先级解析
  ├─ 步骤10：nlu 标题提取/默认值/校验/置信度
  ├─ 步骤11：nlu.parse() 主函数
  ├─ 步骤12：test_nlu.py 20 条用例验证
  └─ 步骤13：reminders 提醒检测
       │
       ▼  P1 验收：自然语言输入 → 确认 → 日程创建 → 提醒
       │
P2: 完善打磨（有余力）
  └─ 预设模式 / 操作历史 / 设置 / 正向反馈
```

---

## 七、关键参考

| 资料 | 位置 |
|------|------|
| 自然语言解析规则 | [本地规则.md](file:///c:/Users/30880/Desktop/AI/AIGC/本地规则.md) |
| 用户操作流程 | [mermaid流程-.md](file:///c:/Users/30880/Desktop/AI/AIGC/mermaid流程-.md) |
| 开发优先级 | [开发顺序建议.md](file:///c:/Users/30880/Desktop/AI/AIGC/开发顺序建议.md) |
| Python sqlite3 文档 | https://docs.python.org/zh-cn/3/library/sqlite3.html |

---

> **一句话总结：我写 4 个 `.py` 核心文件（db / timeline / nlu / reminders），每个文件是纯 Python 函数，输入输出是字典。队友直接 import 调用，拿到数据渲染界面。零框架、零第三方依赖。**
